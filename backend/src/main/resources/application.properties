spring.application.name=health-forum

health_forum.jwt.secret=${HEALTH_FORUM_JWT_SECRET:DMSAJLDNASKFJASNKJFANKAS}
health_forum.frontend.url=${HEALTH_FORUM_FRONTEND_URL:http://localhost:4200}
health_forum.cors.allowed-origins=${HEALTH_FORUM_CORS_ALLOWED_ORIGINS:http://localhost:4200}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/health-forum}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.format_sql=true
server.port=${SERVER_PORT:8080}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.properties.hibernate.transaction.timeout=10
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migrations/data,classpath:db/migrations/repeatable
spring.flyway.baseline-on-migrate=true

#za email
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:alexandar1vanov0907@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:shurgudpxpltfjkb}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true